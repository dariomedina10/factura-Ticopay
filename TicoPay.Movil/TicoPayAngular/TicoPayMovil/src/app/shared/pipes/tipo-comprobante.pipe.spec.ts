import { TipoComprobantePipe } from './tipo-comprobante.pipe';

describe('TipoComprobantePipe', () => {
  it('create an instance', () => {
    const pipe = new TipoComprobantePipe();
    expect(pipe).toBeTruthy();
  });
});
